// 鼠标事件修饰符
    // 1、stop：阻止事件冒泡向上传播
    // 2、prevent：阻止事件的默认行为
    // 3、once：只执行一次
    // 4、self：只有点击自己是才会执行
    // 5、capture：控制事件在捕获阶段执行
    // 6、passive：修饰onscroll事件，不会等到事件的方法执行完，就会立即执行下一次，提高了代码的性能

// 键盘事件修饰符
    // 1、enter
    // 2、tab
    // 3、delate